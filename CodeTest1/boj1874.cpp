#include<bits/stdc++.h>	

using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);

	stack<int> S;
	string Word;

	int N;
	int L=0;
	cin >> N;

	for (int i = 0; i < N; i++)
	{
		int A;
		cin >> A;

	}

	if (0 == S.size())
	{
		cout << "NO" << "\n";

		return 0;
	}

	for (int i = 0; i < Word.length()-1; i++)
	{
		cout << Word[i] << "\n";
	}


}

